1: [x] Add automated code formatting (Rustfmt) and linting (Clippy) on commit using pre-commit hooks.
2: [x] Implement a more explicit/flexible Dependency Injection system (similar to FastAPI's `Depends`).
3: [x] Add support for declarative Background Tasks (similar to FastAPI's `BackgroundTasks`).
4: [x] Integrate WebSockets support.
5: [x] Implement Startup and Shutdown event handlers for application lifecycle management.
6: [x] Develop integrated Authentication and Authorization helpers (e.g., JWT, OAuth2).
7: [x] Create a testing client or utilities for easier API endpoint testing.
8: [x] Streamline/enhance Middleware integration (e.g., more explicit API or common middleware examples).
9: [x] Enhance explicit and declarative HTTP Status Code handling for responses.
10: [x] Streamline File Uploads with a dedicated abstraction (similar to FastAPI's `UploadFile`).
11: [x] Implement a structured HTTPException-like error handling mechanism.
12: [x] Integrate a declarative Security dependency injection mechanism (similar to FastAPI's `Security()`).
13: [x] Create an `APIRouter`-like class for modular API organization and route prefixing.
14: [x] Provide a unified `Request` object for comprehensive access to raw request details.
15: [x] Expose `HTTPConnection`-like details (client/server addresses, etc.) for granular connection access.
16: [x] Implement a unified `Response` class for explicit control over response types and headers.
17: [x] Implement custom response classes (e.g., File, HTML, Redirect, Streaming) for specialized response types.
18: [x] Implement data encoding utilities (similar to FastAPI's `jsonable_encoder`) for consistent JSON serialization.
19: [x] Add a `StaticFiles`-like handler for serving static assets.